import { Component } from '@angular/core';

import { TemplateWrapper } from '../../models/templates/template-wrapper.model';

@Component({
  selector: 'app-template-wrapper',
  template: '',
})
export class TemplateWrapperComponent implements TemplateWrapper {
  data: any;
}
